/** @type {import('tailwindcss').Config} */
export default {
    content: ["./src/**/*.{html,js,jsx}"], // must keep this for Tailwind to work
    theme: {
        extend: {}, // empty now, fine
    },
    plugins: [],
}
